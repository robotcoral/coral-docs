name: Version sync

on:
  repository_dispatch:
    types: [sync_version]

  workflow_dispatch:

jobs:
  sync_version:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - run: echo "I am here"
